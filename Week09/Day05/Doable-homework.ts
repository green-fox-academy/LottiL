/* Create 5 trees
Store the data of them in variables in your program
for every tree the program should store its'
type
leaf color
age
sex
you can use just variables, or lists and/or maps */

type tree = {
    type: string,
    leafColor: string,
    age: number,
    sex: string;
}

let árpádTölgy: tree = {
    type: "oak",
    leafColor: "green",
    age: 800,
    sex: "female",
}

let aLegnagyobbNyár: tree = {
    type: "poplar",
    leafColor: "green",
    age: 100,
    sex: "male",
}

let libanoniCédrus: tree = {
    type: "Lebanese cedar",
    leafColor: "green",
    age: 120,
    sex: "female",
}

let hárs: tree = {
    type: "linden",
    leafColor: "green",
    age: 700,
    sex: "male",
}

let normafa : tree = {
    type: "beech",
    leafColor: "green",
    age: 400,
    sex: "male",
}